using Gtk 4.0;
using Adw 1;

template $GraphsStyleEditorWindow : Adw.Window {
  title: _("Error - No Style selected");
  default-width: 1200;
  default-height: 700;
  close-request => $on_close_request();

  Adw.OverlaySplitView split_view {
    sidebar: Adw.ToolbarView {
      [top]
      Adw.HeaderBar editor_headerbar {
        styles ["flat"]
      }

      content: ScrolledWindow {
        hscrollbar-policy: never;
        vexpand: true;
        Adw.Clamp editor_clamp {
          margin-bottom: 12;
          margin-top: 12;
          margin-start: 12;
          margin-end: 12;
        }
      };
    };

    content: Adw.ToolbarView {
      [top]
      Adw.HeaderBar figure_headerbar {
        styles ["flat"]
      }

      content: Adw.StatusPage {
          icon-name: "dialog-error-symbolic";
          title: _("Canvas Failed to Load");
        };
    };
  }
}