using Gtk 4.0;
using Adw 1;

Adw.AlertDialog invalid_project_dialog {
  heading: _("Invalid Project");
  responses [
    close: _("Close"),
  ]
  close-response: "close";
  default-response: "close";
}